const Checkout = () => {
  return (
    <div>
      <h3 style={{ margin: 20 }}>Checkout page</h3>
      <div style={{ margin: 20, width: 1000 }}>
        A checkout page refers to any website pages shown to a customer during
        the step-by-step checkout process. Think of a checkout pages as the
        online version of a physical checkout counter in a grocery store.
        Checkout pages come in two types: one-page checkout and multi-page
        checkout.
      </div>
    </div>
  );
};

export default Checkout;
